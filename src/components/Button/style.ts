import styled from 'styled-components';

export const Container = styled.div`
  button{
    border:none;
  }

  button[type=submit]{
    margin:16px;
    padding:16px;

    border-radius:8px;

    cursor: pointer;
  }
`;
